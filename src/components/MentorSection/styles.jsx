import { makeStyles } from "@material-ui/core";

export default makeStyles({
  paper: {
    position: "relative",
    marginTop: "50px",
    paddingTop: "50px",
    backgroundColor: "#fffdd0"
  },
  header: {
    letterSpacing: "12px",
    textAlign: "center"
  }
});

import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  root: {
    backgroundColor: "#fffdd0"
  },
  header: {
    padding: "40px 0 ",
    margin: "auto",
    letterSpacing: "12px"
  }
});
